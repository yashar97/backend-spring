<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.clientes_backend.mapper.ProductoRepository">

    <select id="findAll" resultType="com.clientes_backend.model.Producto">
        SELECT id, nombre, categoria, stock, precio
        FROM Producto
    </select>

</mapper>